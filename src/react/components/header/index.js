import React from 'react'
// UI : Material navbar.
import Navigation from './navigation'

/**
 * UI header.
 *
 * Include navigation (Material UI navbar).
 */
const Header = () => (
  <header>
    <Navigation />
  </header>
)

export default Header
